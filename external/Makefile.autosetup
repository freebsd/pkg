include @builddir@/mk/defs.mk
DIRS=	blake2 picosat linenoise sqlite libucl liblua msgpuck yxml libcurl
@if libmachista
DIRS+=	libmachista
@endif
@if libelf-internal
DIRS+=	libelf
@endif
include $(MK)/dir.mk
